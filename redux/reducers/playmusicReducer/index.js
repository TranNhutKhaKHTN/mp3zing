const initialState = {
  data: []
}

const PlayMusicReducer = (state = initialState, action) => {
  switch (action.type) {
    case "":
      return state
    default:
      return state
  }
}
export default PlayMusicReducer